<nav class="navbar navbar-expand-lg bg-secondary position-sticky top-0">
  <div class="container-fluid">
    <a class="navbar-brand text-primary" href="/">
      {% if branding.brand %}
      {{ branding.brand | safe }}
      {% else %} Lorem ipsum {% endif %}
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div
      class="collapse navbar-collapse mx-2 flex-lg-row-reverse"
      id="navbarNavAltMarkup"
    >
      {% if navLinks.links %}
      <div class="navbar-nav">
        {% for link in navLinks.links %}
        <a class="nav-link" href="{{ link.url }}">
          {{ link.title }}
        </a>
        {% endfor %}
      </div>
      {% endif %}
    </div>

    <!-- <div
      class="collapse navbar-collapse mx-2 flex-lg-row-reverse"
      id="navbarNavAltMarkup"
    >
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="/">Home</a>
        <a class="nav-link" href="/pages/blog/">Blog</a>
        <a class="nav-link" href="/pages/projects/">Projects</a>
        <a class="nav-link text-primary" href="/pages/about/">About</a>
      </div>
    </div> -->
  </div>
</nav>
